# Extra deps for hemit/ comparison models (GAN baselines + flow matching).
# Install in pix2pix_cuda or a dedicated env after base requirements.txt / setup script.
#
#   pip install -r requirements-hemit-extra.txt

lightning>=2.0
# pix2pix_cuda pins numpy==1.22 → cannot use monai>=1.4 (needs numpy>=1.24).
# For vanilla_fm in pix2pix_cuda: bash scripts/install_vanilla_fm_monai.sh
#   (monai 1.3.x + monai-generative for DiffusionModelUNet)
# For hemit_compare env only: monai>=1.4,<2 is OK if numpy is bumped there.
monai>=1.3,<1.4
monai-generative>=0.2.3,<0.3
torchmetrics>=1.0
lpips>=0.1.4
huggingface_hub>=0.20
scikit-image>=0.19
scipy>=1.7

# Optional: StarDist cell-masked metrics in eval_baseline.py
# stardist>=0.8.5
