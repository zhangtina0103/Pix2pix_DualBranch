# In-repo HEMIT comparison models (hemit/ package — no vs_v2).
HEMIT_COMPARISON_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="${REPO_ROOT:-$(cd "${HEMIT_COMPARISON_DIR}/../.." && pwd)}"
